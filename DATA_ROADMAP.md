# DATA_ROADMAP — які економічні дані збирати далі

Дорожня карта розширення дослідницької платформи ринку е/е (кейс Київстару:
сонце + батареї). Кожен пункт прив'язаний до аналітичної мети, з оцінкою
джерела, способу доступу та складності.

**Метод інтеграції (для всіх нових джерел):** той самий цикл, що вже працює —
спершу подивитись реальну відповідь API/файлу → написати парсер → протестувати →
залити у GitHub → `git pull` на сервері → за потреби додати в cron.

**Доступ із сервера (відоме):** НБУ, Open-Meteo, GitHub — доступні. `ua.energy`
та вхідна точка деяких сайтів за Cloudflare — блокують дата-центр (потрібне ручне
завантаження). Нові домени позначено `[перевірити з сервера]`.

---

## Уже зібрано (база)

РДН (ціни+криві), ВДР, небаланси, маржинальні ціни БР, курс EUR/USD,
погода (5 локацій), макро НБУ (інфляція + платіжний баланс). У плані — ENTSO-E
(перетоки, генерація за типами, навантаження) після видачі токена.

---

## Пріоритет 1 — драйвери ціни: паливо та вуглець

**Що:** газ (TTF, європейський хаб), вугілля (API2/ARA), вуглець (EUA, EU ETS),
опційно нафта Brent.

**Навіщо:** ці ринки задають **маржинальну вартість** генерації, що формує
рівень і волатильність цін РДН/БР. Без них не пояснити, *чому* ціна така, і
неможливо побудувати forward-погляд (прогноз цін → оцінка майбутніх доходів
сонця й арбітражу батареї). Газ і вугілля — паливо граничних блоків; EUA через
парність з ЄС впливає на імпортні ціни.

**Джерело / доступ:** безкоштовний *щоденний* ряд по TTF/EUA — найскладніша
частина. Кандидати (перевірити доступність і формат):
- Trading Economics — безкоштовний API-ключ (ліміт), має TTF, coal, EUA.
- EEX — частина даних із затримкою безкоштовно.
- Фінансові котирування (ф'ючерси) — перевірити, які тикери реально віддають
  TTF/EUA через відкритий JSON. `[перевірити з сервера]`

**Складність:** низька-середня (логіка проста, питання — знайти стабільний
безкоштовний endpoint). **Пріоритет: високий.**

---

## Пріоритет 2 — облікова ставка НБУ (ставка дисконтування)

**Що:** облікова ставка НБУ (і, за бажання, ставки за операціями НБУ).

**Навіщо:** **ставка дисконту** для NPV/IRR інвестиційного кейсу батареї та
сонця; контекст вартості капіталу в Україні. Прямо потрібна для фінансової
частини рекомендації Київстару.

**Джерело / доступ:** **вже наявний `nbu_macro_collector.py`** — додати потрібний
`apikod` (знайти у списку 44 блоків через `statdirectory?json`; кандидати
пов'язані з «процентні ставки за операціями НБУ»). Той самий механізм, що й
інфляція/BoP.

**Складність:** тривіальна (один блок у наявному збирачі). **Пріоритет: високий.**

---

## Пріоритет 3 — довідник цінових стель (НКРЕКП)

**Що:** історія цінових стель (price caps) РДН та БР за періодами дії — окрема
довідкова таблиця `price_caps`.

**Навіщо:** стелі змінювалися в часі (ми це фіксували: насичення цінової стелі
у 2026 ≈86% вечірніх годин). Без урахування цього аналіз трендів спотворюється —
зростання «середньої ціни» частково є зростанням стелі. Дає змогу рахувати медіани
та частку годин біля стелі коректно.

**Джерело / доступ:** постанови НКРЕКП (PDF). Вручну — невелика таблиця
(дата_від, дата_до, період_доби, стеля). `[ручне, рідко]`

**Складність:** низька (мало рядків). **Пріоритет: середній-високий** (важливо
для якості аналізу, але обсяг даних малий).

---

## Пріоритет 4 — ENTSO-E (уже в плані, чекаємо токен)

**Що:** сусідні DA-ціни (PL/HU/SK/RO), генерація за типами (фактичні сонце/вітер),
навантаження, транскордонні фізичні перетоки.

**Навіщо:** цінова парність імпорт/експорт (драйвер вечірнього піку й денного
провалу), **реальний національний** capture price сонця/вітру (а не лише
GHI-проксі по Львівщині), баланс системи (дефіцит/профіцит).

**Джерело / доступ:** `web-api.tp.entsoe.eu` — безкоштовний токен (запит поштою,
~3 робочі дні). Покриття України під час війни перевіримо емпірично.

**Складність:** середня. **Пріоритет: високий** (щойно буде токен).

---

## Пріоритет 5 — форвардна крива / двосторонні контракти (UEEX)

**Що:** ціни форвардних/двосторонніх аукціонів Української енергетичної біржі.

**Навіщо:** бенчмарк хеджування й контрактної вартості сонячної генерації; основа
для оцінки PPA-подібних угод і ризик-менеджменту доходів.

**Джерело / доступ:** `ueex.com.ua` — `[перевірити з сервера]` (можливо за
Cloudflare, як `ua.energy` → тоді ручне завантаження). Структуру з'ясувати.

**Складність:** середня. **Пріоритет: середній.**

---

## Пріоритет 6 — попит і ширший макро

**Що:** електроспоживання (Держстат/Укренерго), промислове виробництво, ВВП,
міжнародні резерви НБУ (`res`/`res1` — уже доступні через наявний збирач).

**Навіщо:** довгострокові драйвери попиту й макроконтекст для прогнозу; резерви/BoP
— стійкість гривні (впливає на гривневі ціни е/е).

**Джерело / доступ:** НБУ (резерви — додати `apikod` у наявний збирач), Держстат
(промвиробництво/ВВП — окреме джерело, перевірити API/формат).

**Складність:** низька (НБУ-блоки) — середня (Держстат). **Пріоритет: середній.**

---

## Підсумкова черговість

1. Паливо + вуглець (TTF / coal / EUA) — найбільший аналітичний важіль.
2. Облікова ставка НБУ — тривіально, потрібна для фінмоделі.
3. Довідник стель НКРЕКП — якість аналізу трендів.
4. ENTSO-E — щойно прийде токен.
5. UEEX форварди — за наявності часу.
6. Попит/макро — фоновий контекст.

> Після пунктів 1–4 даних достатньо для повноцінного **бізнес-кейсу батареї/сонця
> Київстару**: рівень і структура цін (пояснені паливом), реальний capture,
> вартість гнучкості, штрафи небалансу, ставка дисконтування та цінові стелі.
